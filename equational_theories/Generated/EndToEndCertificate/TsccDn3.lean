import equational_theories.EndToEnd.Load

/-! Generated by equational_theories/Generated/EndToEndCertificate/src/generate.py. Do not edit. -/

/-! `tsccdn` entries 2403-2974.

An entry is a chain from the SCC representative to equation index i.

The data itself is in `json/sccDn.json`, beside this module. The number
after the path is that file's FNV-1a hash, checked on every elaboration because
Lake cannot track a file read during one. See `EndToEnd/Load.lean`.

`RArray` indices are *relative*: `.branch p l r` sends index `n` to `l` when
`n < p`, and to `r` at `n - p` (see `RArray.get`). So the numbers after
`.branch` are left-subtree **sizes**, not indices, and every subtree --
including this shard -- is indexed from 0. Add 2403 to get an index into
`tsccdn`.
-/

namespace EndToEnd

set_option maxRecDepth 20000 in
/-- 572 entries, loaded from `json/sccDn.json`. -/
def tsccdn3 : RArray (List Nat) :=
  loadNatListTable% "json/sccDn.json" 15935514372909340394
    entries 2403 to 2975

end EndToEnd
